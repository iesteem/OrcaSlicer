# 防渗料（Ooze prevention）

此选项会降低非活动挤出机的温度以防止渗料。

## 温度变化

挤出机处于非活动状态时施加的温度差。当耗材设置中的 'idle_temperature' 设为非零值时，不使用此值。

## 预热时间

为了减少换工具后的等待时间，Orca 可以在当前工具仍在使用时预热下一个工具。此设置指定预热下一个工具的时间（秒）。Orca 会插入一条 M104 命令提前预热该工具。

## 预热步数

插入多条预热命令（例如 M104.1）。仅对 Prusa XL 有用。其他打印机请设为 1。
